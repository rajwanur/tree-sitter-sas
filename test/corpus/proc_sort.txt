================================================================================
PROC SORT simple BY
================================================================================
proc sort data=mydata;
  by name;
run;
--------------------------------------------------------------------------------

(source_file
  (proc_step
    (proc_sort_step
      options: (sort_options
        (sort_option
          (sort_option_key)
          (identifier)))
      body: (proc_body
        (by_statement
          (identifier))))))

================================================================================
PROC SORT with WHERE
================================================================================
proc sort data=mydata;
  where age > 10;
  by name;
run;
--------------------------------------------------------------------------------

(source_file
  (proc_step
    (proc_sort_step
      options: (sort_options
        (sort_option
          (sort_option_key)
          (identifier)))
      body: (proc_body
        (where_statement
          (binary_expression
            left: (identifier)
            right: (number)))
        (by_statement
          (identifier))))))

================================================================================
PROC SORT with OUT option
================================================================================
proc sort data=mydata out=sorted;
  by name;
run;
--------------------------------------------------------------------------------

(source_file
  (proc_step
    (proc_sort_step
      options: (sort_options
        (sort_option
          (sort_option_key)
          (identifier))
        (sort_option
          (sort_option_key)
          (identifier)))
      body: (proc_body
        (by_statement
          (identifier))))))

================================================================================
PROC SORT with NODUPKEY option
================================================================================
proc sort data=mydata nodupkey;
  by name;
run;
--------------------------------------------------------------------------------

(source_file
  (proc_step
    (proc_sort_step
      options: (sort_options
        (sort_option
          (sort_option_key)
          (identifier))
        (sort_option_flag))
      body: (proc_body
        (by_statement
          (identifier))))))

================================================================================
PROC SORT with value options and boolean flags
================================================================================
proc sort data=mydata out=sorted dupout=dups nodupkey force equals threads tagsort;
run;
--------------------------------------------------------------------------------

(source_file
  (proc_step
    (proc_sort_step
      options: (sort_options
        (sort_option
          (sort_option_key)
          (identifier))
        (sort_option
          (sort_option_key)
          (identifier))
        (sort_option
          (sort_option_key)
          (identifier))
        (sort_option_flag)
        (sort_option_flag)
        (sort_option_flag)
        (sort_option_flag)
        (sort_option_flag)))))

================================================================================
PROC SORT with unknown option key (linter fallback)
================================================================================
proc sort bogusopt=x;
run;
--------------------------------------------------------------------------------

(source_file
  (proc_step
    (proc_sort_step
      options: (sort_options
        (sort_option
          (sort_option_key
            (identifier))
          (identifier))))))
